import * as React from 'react';
import { connect } from 'react-redux';
//import moment from 'moment';
//import './intro.scss';
export class ReleasesNotes extends React.Component<any> {
 

    state = {}
    public render() {

        return (
<></>
        )
    }
}
const mapStateToProps = (state: any) => ({

});


export default connect<any, any, any>(mapStateToProps)(ReleasesNotes);